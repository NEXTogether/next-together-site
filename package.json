{
  "name": "next_together_landing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:turbo": "next dev --turbopack",
    "build": "next build",
    "build:prod": "NODE_ENV=production next build",
    "start": "next start",
    "export": "next export",
    "gcloud:deploy": "gcloud app deploy",
    "gcloud:deploy-run": "gcloud run deploy next-together-site --source . --region=europe-west1 --allow-unauthenticated",
    "gcloud:build": "gcloud builds submit --config cloudbuild.yaml",
    "docker:build": "docker build -t next-together-site .",
    "docker:run": "docker run -p 8080:8080 next-together-site"
  },
  "dependencies": {
    "next": "15.5.2",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}